export class TokenStore {
  // Securely manages access and refresh tokens using OS keychain
  public async getToken(provider: string) {
    // Scaffold
  }
  public async setToken(provider: string, token: string) {
    // Scaffold
  }
}
